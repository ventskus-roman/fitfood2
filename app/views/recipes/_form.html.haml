= simple_form_for(@recipe, html: { class: "form-horizontal" }) do |f|
  = f.input :name, label: "Название", input_html: { class: "form-control" }
  = f.input :description, label: "Как это приготовить?", input_html: { class: "form-control" }
  = f.input :image, as: :file, label: "Загрузить фото рецепта"
  %br/

  = f.button :submit, "Сохранить"
